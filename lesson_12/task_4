def delivery():
    quantity = int(input('number of goods: '))
    if quantity == 0:
        print(0)
    if quantity == 1:
        print(100)
    if quantity > 1:
        print(quantity * 50 + 50)

delivery()
