sec_all = int (input('please enter the number of seconds: '))
secinp = sec_all
sec = sec_all % 60
sec_all -= sec

min_all = sec_all / 60
min = int(min_all % 60)
hour = int(min_all // 60)

print(secinp, 'second(s) - this is', hour, 'hour(s),', min, 'minute(s)', sec, 'second(s)')
