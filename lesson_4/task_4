r = int(input('Please enter the price of the ball(rub): '))
k = int(input('Please enter the price of the ball(kop): '))
n = int(input('Please enter the number of balls: '))

kop = k * n % 100
rub = r * n + k * n // 100

print('The price for', n, 'ball(s) is', rub, 'rub.', kop, 'kop.')
