first = int(input('first_number: '))
second = int(input('second_number: '))
if first >= second:
    for i in range(second, first+1):
        if second % 2 == 0:
            print(second)
        second += 1
else:
    for i in range(first, second+1):
        if first % 2 == 0:
            print(first)
        first += 1
