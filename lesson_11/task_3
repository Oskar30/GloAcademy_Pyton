ip = input('input ip: ')
iplist = ip.split('.')
f = 0
for j in range(1):
    for i in range(len(iplist)):
        if int(iplist[i]) < 0 or int(iplist[i]) > 255:
            print('NO')
            f += 1
            break
    if f > 0:
        break
    else:
        print('YES')
